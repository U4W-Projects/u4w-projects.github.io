---
---
<section id="services" class="services-section">
    <div class="container">
        <h2 class="section-title">OUR DOMAINS</h2>
        <div class="services-grid">
            <div class="service">
                <div class="service-icon">
                    <i class="fas fa-vial"></i>
                </div>
                <h3>Experimental Tech</h3>
                <p>Exploring and developing pioneering technologies for space exploration.</p>
            </div>
            <div class="service">
                <div class="service-icon">
                    <i class="fas fa-robot"></i>
                </div>
                <h3>Cosmic AI Systems</h3>
                <p>Building AI and machine learning platforms for interstellar problems.</p>
            </div>
            <div class="service">
                <div class="service-icon">
                    <i class="fas fa-cogs"></i>
                </div>
                <h3>Quantum Hardware</h3>
                <p>Developing quantum computing systems for space applications.</p>
            </div>
            <div class="service">
                <div class="service-icon">
                    <i class="fas fa-shapes"></i>
                </div>
                <h3>Prototype Forging</h3>
                <p>Rapid prototyping and building models for space technology concepts.</p>
            </div>
            <div class="service">
                <div class="service-icon">
                    <i class="fas fa-server"></i>
                </div>
                <h3>Quantum Cloud</h3>
                <p>Designing quantum-resistant cloud for secure space communication.</p>
            </div>
            <div class="service">
                <div class="service-icon">
                    <i class="fas fa-satellite"></i>
                </div>
                <h3>Deep Space Comms</h3>
                <p>Developing communication systems for astronomical distances.</p>
            </div>
        </div>
    </div>
</section>

<style>
    .services-section {
        background-color: rgba(15, 23, 42, 0.8);
        position: relative;
        border-top: 1px solid var(--border-color);
        border-bottom: 1px solid var(--border-color);
        transition: all var(--transition-speed);
    }

    :global(.light-mode) .services-section {
        background-color: rgba(245, 245, 245, 0.8);
    }
    
    .services-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 30px;
        position: relative;
        z-index: 1;
    }
    
    .service {
        text-align: center;
        padding: 40px 25px;
        border-radius: 10px;
        background-color: var(--card-bg);
        transition: all 0.3s;
        border: 1px solid var(--border-color);
        backdrop-filter: blur(5px);
        position: relative;
    }
    
    .service:hover {
        transform: translateY(-8px);
        box-shadow: 0 15px 30px var(--shadow-color);
        border-color: rgba(16, 185, 129, 0.3);
    }

    .service::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 3px;
        background-color: var(--accent-2);
        transform: scaleX(0);
        transition: transform 0.3s;
    }

    .service:hover::after {
        transform: scaleX(1);
    }
    
    .service-icon {
        width: 70px;
        height: 70px;
        margin: 0 auto 25px;
        background: linear-gradient(135deg, var(--primary-color), #1D4ED8);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.8rem;
        transition: all 0.3s;
    }

    :global(.light-mode) .service-icon {
        box-shadow: 0 0 10px rgba(30, 58, 138, 0.2);
    }

    :global(:not(.light-mode)) .service-icon {
        box-shadow: 0 0 15px rgba(59, 130, 246, 0.3);
    }

    .service:hover .service-icon {
        background: linear-gradient(135deg, var(--accent-2), #0D9488);
        transform: rotate(10deg);
    }
    
    .service h3 {
        font-size: 1.3rem;
        color: var(--text-primary);
        margin-bottom: 15px;
        font-weight: 600;
        transition: color var(--transition-speed);
    }
    
    .service p {
        color: var(--text-secondary);
        font-size: 1rem;
        line-height: 1.6;
        transition: color var(--transition-speed);
    }
    
    @media (max-width: 1200px) {
        .services-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    @media (max-width: 768px) {
        .services-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
